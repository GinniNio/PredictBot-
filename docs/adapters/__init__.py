"""See `docs/__init__.py`'s own docstring -- this file exists only so
`docs.adapters.data` can be packaged into the wheel. `docs/adapters/`
otherwise holds plain Markdown (e.g. `SOCCER_1X2_ADAPTER_SPEC.md`), never
packaged.
"""
