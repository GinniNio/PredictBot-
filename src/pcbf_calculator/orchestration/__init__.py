"""One-command orchestration pipelines that reuse -- never reimplement --
this platform's own ingestion, screening, pricing and forecasting
functions. See ``bet9ja_research_session.py`` for the first such
pipeline.
"""
